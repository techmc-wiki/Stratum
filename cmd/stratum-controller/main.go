package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stdout, "stratum-controller MVP stub: HTTP API and persistence are not started yet")
	// TODO: load configuration, repositories, services, and the HTTP API.
}
